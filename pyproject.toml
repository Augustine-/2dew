[project]
name = "flaskr"
version = "1.0.0"
description = "It's the Flask tutorial blog."
dependencies = [
    "flask",
]

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"